# 功能清单

## 概述

本项目是一个多提供商的大语言模型（LLM）客户端，支持 OpenAI、Anthropic 和智谱 AI 提供商，提供统一的流式对话 API。

## 核心功能

### 1. 多提供商支持

#### 已实现提供商
- ✅ **OpenAI**
  - 默认模型: `gpt-4.1`
  - API 基础地址: `https://api.openai.com/v1`
  - 环境变量: `OPENAI_API_KEY`, `OPENAI_BASE_URL`

- ✅ **Anthropic**
  - 默认模型: `claude-sonnet-4.5`
  - API 基础地址: `https://api.anthropic.com/v1`
  - 环境变量: `ANTHROPIC_API_KEY`, `ANTHROPIC_BASE_URL`
  - API 版本: `2023-06-01` (需更新至 `2024-01-01`)

- ✅ **智谱 AI (Zhipu-Coding)**
  - 默认模型: `glm-4.7`
  - API 基础地址: `https://open.bigmodel.cn/api/coding/paas/v4`
  - 环境变量: `ZHIPU_CODING_API_KEY`

#### 扩展性
- 🔄 统一的 `ProviderClient` 接口
- 🔄 共享的 `openai-compatible` 基础层
- 📦 模块化设计，易于添加新提供商

---

### 2. 流式对话 API

#### 流式事件类型
```typescript
- token: { type: 'token', text: string }      // 文本生成事件
- tool_call: { type: 'tool_call', toolCall }  // 工具调用事件
- done: { type: 'done' }                      // 完成事件
```

#### 输入参数
- `model`: 模型名称
- `messages`: 对话消息历史
- `summary`: 对话摘要（可选）
- `systemPrompt`: 系统提示词（可选）
- `tools`: 可用工具列表（可选）
- `maxTokens`: 最大生成 Token 数（可选）
- `signal`: AbortSignal 用于取消请求（可选）

---

### 3. 上下文管理

- 📝 **对话历史截断** (`context/truncation.ts`)
  - 智能截断过长的对话历史
  - 保留关键上下文信息
  - 支持摘要注入

- 🧹 **消息规范化**
  - 统一不同提供商的消息格式
  - 处理系统提示词
  - 分离系统消息与对话消息

---

### 4. 工具调用支持

#### 工具定义
```typescript
{
  type: 'function',
  function: {
    name: string,
    description: string,
    parameters: unknown
  }
}
```

#### 工具调用事件
- 支持流式接收工具调用
- 返回工具调用 ID、名称和参数

---

### 5. 配置管理

#### 模型配置
- OpenAI: `gpt-4.1`, `gpt-4.1-mini`
- Anthropic: `claude-sonnet-4.5`
- 智谱 AI: `glm-4.7`, `glm-5.1`, `glm-5-turbo`, `glm-4.5-air`

#### 设置选项
- API Key 配置
- 自定义 API 基础地址
- 默认模型设置
- 自定义 fetch 实现

---

## 技术特性

### 类型安全
- ✅ 完整的 TypeScript 类型定义
- ✅ Provider 流事件类型约束
- ✅ 严格的接口定义

### 网络处理
- ✅ SSE (Server-Sent Events) 流式解析
- ✅ AbortSignal 请求取消支持
- ✅ 错误处理和响应验证

### 可扩展性
- 🔄 提供商注册机制
- 🔄 统一的错误处理
- 🔄 共享的工具函数库

---

## 待实现功能

### 优先级高
- [ ] 更新 Anthropic API 版本至 `2024-01-01`
- [ ] 统一消息规范化逻辑
- [ ] 添加重试机制
- [ ] 实现速率限制处理

### 优先级中
- [ ] 添加提供商健康检查
- [ ] 实现请求缓存
- [ ] 添加日志记录系统
- [ ] 优化错误信息

### 优先级低
- [ ] 添加更多 OpenAI 兼容提供商
- [ ] 实现模型能力元数据（上下文窗口、价格等）
- [ ] 添加性能监控
- [ ] 支持批量请求

---

## 已知问题

1. **Anthropic Provider**
   - API 版本过时（`2023-06-01`）
   - 缺少共享错误处理逻辑
   - 自定义实现与 OpenAI 兼容层重复

2. **消息规范化**
   - 各提供商独立实现
   - 缺少统一的规范化工具

3. **配置管理**
   - 缺少模型能力元数据
   - 缺少验证机制

---

## 架构文件

```
src/providers/
├── types.ts                    # 核心类型定义
├── openai.ts                   # OpenAI provider
├── anthropic.ts                # Anthropic provider
├── zhipu-coding.ts             # 智谱 AI provider
├── openai-compatible.ts        # OpenAI 兼容层基础
└── sse.ts                      # SSE 流式解析

src/context/
└── truncation.ts               # 对话历史截断逻辑

settings.ts                     # 配置文件
```

---

## 使用示例

```typescript
import { createOpenAIClient } from './providers/openai.ts'

const client = createOpenAIClient({
  apiKey: 'your-api-key',
  defaultModel: 'gpt-4.1'
})

for await (const event of client.stream({
  model: 'gpt-4.1',
  messages: [{ role: 'user', content: 'Hello!' }],
  systemPrompt: 'You are a helpful assistant.'
})) {
  if (event.type === 'token') {
    console.log(event.text)
  }
}
```